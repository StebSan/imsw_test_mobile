import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'imsw_test_mobile',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
